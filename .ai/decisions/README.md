# Architecture Decision Records

Use an ADR for a decision that constrains future work, has meaningful alternatives, or would otherwise be repeatedly debated.

Statuses: `Proposed`, `Accepted`, `Superseded`, `Rejected`.

Never rewrite an accepted decision to hide history. Create a new ADR and mark the old one superseded.
